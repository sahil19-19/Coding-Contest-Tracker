<!DOCTYPE html>
<%- include("header")-%>

<body>
                <%var k=0%>
                    <% for(var i=0;i<arr.length;i++){ %>
                        <% if( (arr[i].host=='codeforces.com' || arr[i].host=='codechef.com' || arr[i].host=='leetcode.com' || arr[i].host=='atcoder.com' ||arr[i].host=='hackerrank.com'|| arr[i].host=='hackerearth.com') && (arr[i].in_24_hours=='Yes')){  %>
                      <% k++ %>
                    <th scope="row"><%=k%></th>
                    <td><a href=<%=arr[i].href%>><%=arr[i].event %></a></td>
                    <td><%=arr[i].start %></td>
                    <td><%=arr[i].end %></td>
                    <td><%=arr[i].duration%></td>
                </tr>
                    <% } %>
                    <% } %>
                </tbody>
            </table>
           <br>
           <br>
            <h2 style="text-align: center; font-size: 1.95rem; font-weight: 700;"><u>UPCOMING</u></h2>
          <br>  
    <table class="table table-striped table-responsive table-hover">
        <thead>
       
            <tr>
                <th scope="col">No.</th>
                <th scope="col">Name</th>
                <th scope="col">Start Time</th>
                <th scope="col">End Time</th>
                <th scope="col">Duration</th>
                <!-- <th scope="col">Link</th> -->
              </tr>
        </thead>
        <tr> 
            <%var k=0%>
            <% for(var i=0;i<arr.length;i++){ %>
                <% if((arr[i].host=='codeforces.com' || arr[i].host=='codechef.com' || arr[i].host=='leetcode.com' || arr[i].host=='atcoder.com' ||arr[i].host=='hackerrank.com'|| arr[i].host=='hackerearth.com')){  %>
              <% k++ %>
            <th scope="row"><%=k%></th>
            <td><a href=<%=arr[i].href%>><%=arr[i].event %></a></td>
            <td><%=arr[i].start %></td>
            <td><%=arr[i].end %></td>
            <td><%=arr[i].duration%></td>
            <!-- <td><a href=<%=arr[i].href%>><%=arr[i].host %></a></td> -->
        </tr>
            <% } %>
            <% } %>
          

        </tbody>
    </table>

    <!-- <script>
        let arr = '<%= arr %>';
        document.querySelector('#constestInputButton').addEventListener(
            'click', ()=>{
                // alert('hello');
                const site = document.querySelector('#constestInput');
                // const siteName = site.textContent.toLocaleLowerCase();
                // console.log(site.value.toLowerCase());
                const siteName = site.value.toLowerCase();
                alert(arr);
                let temp = arr.filter(helper);
                function helper(val){
                    siteName =  siteName + ".com";
                    return val.site == siteName;
                }
                alert(temp);
            }
        )
    </script>   -->
</body>

</html>
<%- include("footer")-%>